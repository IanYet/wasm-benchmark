import { M3 } from './utils'

export function perspectiveTransform(srcPtr: i32, sW: i32, sH: i32, dW: i32, dH: i32, m: M3): i32 {
	const sPixelCount: i32 = sW * sH * 4
	return 0
	// return (inputPtr + width + height) as i32
}
